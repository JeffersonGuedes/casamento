services:
  web:
    build:
      context: .
      dockerfile: dockerfile
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - media_data:/app/media
    restart: unless-stopped

volumes:
  media_data: